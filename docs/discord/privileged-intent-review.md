# Discord 特権インテント審査 申請パック（ロボポリス）

Discord から「10,000ユーザー到達につき特権インテントの審査申請が必要」という通知が届いたための申請資料です。
残り猶予は通知時点で **29日**。Developer Portal → 該当アプリ → **Bot** タブ → *Privileged Gateway Intents* セクションの
**Apply for Access** ボタンから申請します。

| 項目 | 値 |
|---|---|
| アプリ名 | ロボポリス (RoboPolice) |
| アプリケーション ID | `308674858781245440` |
| ベース | Logger v3 (AGPL-3.0) のフォーク |
| ソース | https://github.com/waxsd100/RoboPolice （public） |
| 公開種別 | **プライベートBOT**（一般公開しておらず、第三者は追加できない） |
| 設置サーバー | 本番のコミュニティサーバー（https://discord.com/invite/nobaman ）＋ **当方運営の非公開テストサーバー数個** |
| 1万ユーザー到達の理由 | 配布数ではなく、**その1サーバーのメンバーが1万人超**であるため |
| プライバシーポリシー | https://github.com/waxsd100/RoboPolice/blob/master/PRIVACY.md |
| 利用規約 | https://github.com/waxsd100/RoboPolice/blob/master/TERMS.md |

> **この申請の性格**：本BOTは1つのコミュニティが自分たちのモデレーションのために自前で動かしている
> プライベートBOTです。審査では「不特定多数に配布されたBOTがデータを集めている」ケースと明確に
> 区別されるよう、**当方が運営するサーバーにしか設置していないこと**を各回答で明示します。
>
> ⚠️ **「1サーバーのみ」とは書かないでください。** 実際には動作確認用のテストサーバーにも入っており、
> Discord 側はアプリの設置サーバー数を把握できます。サーバー数を正確に述べる必要はありませんが、
> 事実と異なる断定は、送信時に同意する「情報が正確かつ完全である」という表明に反します。
> 「当方が運営する少数のサーバーのみ」という書き方であれば、テストサーバーが増減しても真であり続けます。

---

## 1. 結論：申請すべきインテントは何か

コードを実地調査した結果です。**使っていないインテントを申請に含めると、申請全体が却下されます**
（Discord は部分承認をせず、1つでも不要と判断されると全体が Denied になります）。

| 特権インテント | 実際の使用 | 申請 | 根拠 |
|---|---|---|---|
| **Server Members** (`GUILD_MEMBERS`) | **使用中** | **申請する** | `src/bot/index.js:65` で有効化。参加/退出/キック/ニックネーム/ロール変更ログの根幹 |
| **Message Content** (`MESSAGE_CONTENT`) | **使用中**（実機確認済み） | **申請する** | 削除/編集メッセージの本文ログに必須。詳細は 1.1 |
| **Presence** (`GUILD_PRESENCES`) | **未使用** | **申請しない** | コード全体で `guildPresences` の参照ゼロ |

### 1.1 Message Content は本番で動作している（実機確認済み）

削除ログに実際のメッセージ本文が出力されることを確認済みのため、**本番のロボポリスは
ゲートウェイで MESSAGE_CONTENT (`1 << 15`) を要求できています**。したがって Message Content は
「実使用中の特権インテント」であり、申請対象で確定です。

一方で、**このリポジトリの内容そのままでは Message Content を要求できません**（申請とは別件の乖離）：

- `src/bot/index.js:60-68` の intents 配列に `messageContent` が無い
- `package-lock.json` が固定している Eris は `curtisf/eris` の commit `c075c5b`（`0.16.2-dev`）で、
  この版の `lib/Constants.js` の `Intents` は `directMessageTyping` (`1 << 14`) で終わっており、
  `messageContent` の定数が存在しない
- `lib/Client.js:176-188` は配列を走査して未知の名前を `warn` して**黙って捨てる**実装のため、
  仮に `'messageContent'` を配列に足しても無視される

この状態で計算されるビットフィールドは `719`（`messageContent` の `32768` を含まない）です。
実機が動いている以上、**本番環境は別バージョンの Eris か、別の intents 指定で稼働しています。**
（Developer Portal 側でも MESSAGE CONTENT INTENT は ON であることを確認済み。2026-09-13）

> **申請への影響：なし。** Message Content を申請対象に含めてください。
> ただし「リポジトリの `master` と本番デプロイが乖離している」こと自体は別途確認を推奨します
> （このリポジトリから `npm ci` で再デプロイすると本文ログが壊れます）。

---

## 2. 各インテントの使用箇所（審査で問われる根拠）

### Server Members Intent

| 機能 | ファイル |
|---|---|
| 参加ログ（アカウント作成日・使用された招待コードの特定） | `src/bot/events/guildMemberAdd.js` |
| 退出／キックログ（Audit Log 照合で退出とキックを判別、所持ロールを記録） | `src/bot/events/guildMemberRemove.js:28-31` |
| ニックネーム変更・ロール変更・メンバー認証通過ログ | `src/bot/events/guildMemberUpdate.js` |
| 他の全ログ埋め込みでの表示名／ニックネーム／アバター解決 | `src/bot/events/messageDelete.js:19-31`, `messageUpdate.js:16` |
| `/userinfo` コマンド | `src/bot/slashcommands/userinfo.js` |
| BAN / BAN解除ログの対象者情報 | `src/bot/events/guildBanAdd.js`, `guildBanRemove.js` |

**代替不可の理由**：`GUILD_MEMBER_ADD` / `REMOVE` / `UPDATE` は特権インテント無しでは一切届きません。
スラッシュコマンドは「ユーザーが実行したとき」しか動かないため、参加・退出という*受動的イベント*の記録には原理的に使えません。

### Message Content Intent

| 機能 | ファイル |
|---|---|
| メッセージをAES-256暗号化してDBへ保存（削除時ログの元データ） | `src/db/interfaces/postgres/create.js:42-64` |
| 削除メッセージの本文表示 | `src/bot/events/messageDelete.js:44-60` |
| 編集の Before / After 差分表示 | `src/bot/events/messageUpdate.js:60-110` |
| 一括削除ログ | `src/bot/events/messageDeleteBulk.js` |
| `/archive`（モデレーターによるチャンネル書き出し） | `src/bot/slashcommands/archive.js:36` |

**代替不可の理由**：削除されたメッセージは Discord API から再取得できません。
モデレーターが荒らし・詐欺リンク・ハラスメントに対処するには、削除される*前*に本文を保持している必要があり、
これはスラッシュコマンドでは実現できません。

---

## 3. 申請フォーム 記入内容（実フォーム構造に対応・2026-09-13 実画面確認済み）

フォームは**条件分岐**します。以下は実際の出現順です。フォームは日本語表示で言語の指定も無いため、
**そのまま日本語で提出して問題ありません**。**下書き保存ボタンが無い**ので、この節を完成させてから
一気に入力してください。

### 3.0 回答サマリ（プルダウンの選択だけ先に確定）

| 設問 | 回答 |
|---|---|
| ② 公開のプライバシーポリシーで、ユーザーにデータ使用について伝えていますか？ | **Yes** |
| ③ どのインテントを申請しますか？ | **Server Members / Message Content の2つだけ**（Presence は**チェックしない**） |
| SM: Discordの外部でAPIデータを保存しますか？ | **Yes** |
| SM: APIデータを30日以内にわたって保存しますか？ | **Yes** |
| SM: 保存中のデータを暗号化しますか？ | **Yes** |
| MC: ユーザーはメッセージ内容データの追跡をオプトアウトできますか？ | **No**（→ §3.5 要判断） |
| MC: メッセージコンテンツデータをプラットフォーム外で保存しますか？ | **Yes** |
| MC: ユーザーメッセージ内容データを30日以内にわたって保存しますか？ | **Yes** |
| MC: 保存中のデータを暗号化しますか？ | **Yes** |
| MC: 機械学習またはAIモデルのトレーニングに使用されますか？ | **No** |

> **「Discordの外部でAPIデータを保存しますか？」は Server Members セクションでも Yes です。**
> メンバーのプロフィールは保存していませんが、保存するメッセージ行には投稿者IDが含まれており、
> これはメンバー由来のAPIデータだからです。ここで No と答えると Message Content セクションの
> Yes と矛盾して見えます。

### 3.1 基本項目

#### ① アプリケーションでは何が行われますか？（2000字）

```
ロボポリスは「プライベート」な Discord アプリケーションです。一般公開しておらず、第三者が
サーバーに追加することはできません。設置されているのは当方が運営するサーバーのみで、内訳は
実際にログを取得しているコミュニティサーバー1つと、変更を本番へ反映する前に動作確認するための
非公開テストサーバー数個です。テストサーバーにはログの出力先を設定していません。本アプリは
削除・編集ログの出力先が設定されていないサーバーではメッセージを保存しないため、メッセージ
データが実際に保存されているのはコミュニティサーバー1つ分だけです。1万ユーザーの基準に達したのは、
そのコミュニティサーバーのメンバーが1万人を超えているためであり、多数のサーバーに配布されている
ためではありません。

本アプリは監査ログBOTです。管理者が /setup でイベント種別ごとにスタッフ専用のログチャンネルを
割り当てると、モデレーション上重要なイベントが発生するたびに、そのチャンネルへ構造化された埋め込みを
投稿します。これによりモデレーターは「サーバーで何が起きたか」を後から確認できます。

記録するイベント: メンバーの参加（アカウント作成からの日数、使用された招待コードを含む）、退出、
キック、BANおよびBAN解除、ニックネーム変更、ロール変更、メンバー認証の通過、メッセージの削除、
メッセージの編集（変更前後）、一括削除、チャンネル／ロール／絵文字／スタンプ／サーバー設定の変更、
招待の作成と削除、ボイスチャンネルの入室・退室・移動。

モデレーター向けコマンド: /setup、/ignorechannel（チャンネルをログ対象外にする）、/stoplogging
（ログを全停止する）、/logbots、/userinfo、/serverinfo、/archive（インシデント報告用に
チャンネルの直近メッセージを書き出す）、/clearmydata（保存データの削除請求方法を案内する）。

本アプリはオープンソースの Logger v3（AGPL-3.0）のフォークです。ソースコードは
https://github.com/waxsd100/RoboPolice で全文公開しており、本申請の記述はすべてコードで検証できます。

Presence インテントは使用していないため、申請しません。
```

#### ② 公開のプライバシーポリシーで…伝えていますか？ → **Yes**

#### ②-1 プライバシーポリシーはどこで閲覧できますか？（2000字）

> プレースホルダーが「Botプロフィール内のリンク、ヘルプコマンド、ルールチャンネル…」と例示して
> いるので、**アプリ内からの導線**を具体的に書くのが正解です。実装済みです。

```
以下の3経路で閲覧できます。

1. アプリ内のコマンド
   - /help の「Privacy Policy」欄にプライバシーポリシーと利用規約へのリンクを表示しています。
   - /info でも同じリンクを表示しています。
   - /clearmydata でデータ削除請求の方法を案内する際に、プライバシーポリシーへリンクしています。
2. 公開リポジトリ上の恒久URL（下記③に記載）。本アプリはソースコードを全文公開しており、
   ポリシーはそのリポジトリ内に置かれているため、改訂履歴も公開されています。
3. 本アプリが稼働しているコミュニティサーバー内の掲示。
```

#### ②-2 プライバシーポリシーへのリンクを共有してください。（2000字）

```
プライバシーポリシー:
https://github.com/waxsd100/RoboPolice/blob/master/PRIVACY.md

利用規約:
https://github.com/waxsd100/RoboPolice/blob/master/TERMS.md

いずれも英語と日本語を併記しています。改訂履歴は
https://github.com/waxsd100/RoboPolice/commits/master/PRIVACY.md で公開されています。
```

#### ③ どのインテントを申請しますか？

**Server Members** と **Message Content Intent** の2つだけにチェック。**Presence はチェックしない。**

### 3.2 Server Members Intent セクション

#### なぜギルドメンバーのインテントが必要ですか？（2000字）

```
メンバーのライフサイクルのログは本アプリで最も使われている機能であり、このインテント無しでは
実現不可能です。GUILD_MEMBER_ADD / GUILD_MEMBER_REMOVE / GUILD_MEMBER_UPDATE は、このインテントが
無ければそもそも配信されません。これらはユーザー操作を伴わない受動的なサーバーイベントであるため、
スラッシュコマンドで代替することは原理的にできません。誰かが黙って退出したとき、コマンドを
実行する契機が存在しないからです。

具体的な用途:

1. 参加ログ — 参加したメンバー、アカウント作成からの日数（荒らし・捨て垢を判断する標準的な指標）、
   サーバーのメンバー数、使用された招待コードを記録します。招待コードの特定は、GUILD_MEMBER_ADD の
   たびにサーバーの招待使用回数とキャッシュを比較することで行っています。
2. 退出・キックログ — GUILD_MEMBER_REMOVE の際に監査ログを参照し、自発的な退出とキックを区別し、
   実行したモデレーターを特定し、そのメンバーが保持していたロールを記録します。これによりスタッフは
   どの権限が失われたかを把握できます。
3. ニックネーム・ロール・メンバー認証のログ — GUILD_MEMBER_UPDATE から取得し、誰がいつ特権ロールを
   得たかを追跡できるようにします。
4. 他のすべてのログ埋め込みにおける表示名の解決 — メンバー情報があることで、削除ログやBANログが
   単なる数値IDではなく「Taro（ニックネーム: Mod-Taro）」と表示できます。
5. モデレーター向けコマンド /userinfo。

メンバーデータは読み取るのみです。メンバーのプロフィール情報（ニックネーム、ロール、アバター）は
データベースに一切書き込んでおらず、ゲートウェイのメモリ上のキャッシュに存在するだけで、再起動時に
破棄されます。永続化しているメンバー由来のデータは、保存するメッセージ行に付随する投稿者IDのみです。
これらはすべて、当方が運営するサーバー内に限定されています。
```

#### 使用例を示すスクリーンショットや動画へのリンク（2000字）

```
<< 参加ログ／退出・キックログ／ニックネーム・ロール変更ログ／/userinfo の4点のURL >>
```

#### Discordの外部でAPIデータを保存しますか？ → **Yes**

##### APIデータを30日以内にわたって保存しますか？ → **Yes**

##### ユーザーがアクティビティデータの削除をリクエストする際の、あなたへの連絡方法は？（2000字）

```
1. アプリ内コマンド: サーバー内で /clearmydata を実行すると、連絡方法とプライバシーポリシーへの
   リンクが表示されます。
2. サポートサーバー: https://discord.com/invite/nobaman
   （本アプリが稼働しているコミュニティサーバーであり、運営スタッフが直接対応します）

請求を受けた場合は、投稿者IDを用いて当該ユーザーのメッセージ行を削除します。また請求の有無に
かかわらず、メッセージ行は30日で自動的に削除されます。
```

##### 開発者ポリシーで要求されている通り、保存中のデータを暗号化しますか？ → **Yes**

### 3.3 Message Content Intent セクション

#### ユーザーはメッセージ内容データの追跡をオプトアウトできますか？ → **No**（§3.5 参照）

#### メッセージコンテンツデータをプラットフォーム外で保存しますか？ → **Yes**

##### ユーザーメッセージ内容データを30日以内にわたって保存しますか？ → **Yes**

##### ユーザーがアクティビティデータの削除をリクエストする際の、あなたへの連絡方法は？

→ §3.2 と同じ文面を貼り付け。

##### 開発者ポリシーで要求されている通り、保存中のデータを暗号化しますか？ → **Yes**

#### メッセージコンテンツデータは機械学習またはAIモデルのトレーニングに使用されますか？ → **No**

#### なぜMessage Content Intentが必要ですか？（2000字）

> オプトアウト・保存期間・保存内容について説明できる自由記述欄はここだけです。
> プルダウンで No と答えた2項目の補足を、必ずここに書いてください。

```
本アプリの中核機能は、削除・編集されたメッセージの内容をモデレーターに提示することです。削除された
メッセージを Discord API から再取得することはできないため、削除が起きる「前」に内容を受け取っておく
以外に方法がありません。モデレーターがコマンドを実行できる時点では、証拠は既に失われています。
したがってスラッシュコマンドやインタラクションによる代替設計は成立しません。

用途は以下に限られます:

1. メッセージ削除ログ — 削除されたメッセージの本文と画像添付をスタッフに表示します。詐欺・
   フィッシングリンク、荒らしスパム、個人情報晒し、ハラスメントへの対処や、他のモデレーターによる
   削除が適切だったかの検証に使われています。
2. メッセージ編集ログ — 変更前後の差分を表示します。「無害な内容を投稿してから広告や詐欺リンクに
   編集する」という、Discord の標準UIでは見えない挙動を検知するために必要です。
3. 一括削除ログ — 同様の目的で、大量削除に対応します。
4. /archive — メッセージ管理権限を持つモデレーターが、インシデント報告のために1チャンネルの
   直近メッセージを書き出せるようにします。

【オプトアウトについて（上記の設問の補足）】
本アプリはサーバーの監査ログであるため、メンバー個人が自分だけログ対象から外れる仕組みはなく、
上記設問には「いいえ」と回答しました。代わりに以下の制御があります。サーバー管理者は
/ignorechannel で任意のチャンネルをログ対象から除外でき、/stoplogging でログを完全に停止でき、
/setup でイベント種別ごとに無効化できます。メンバーには、本サーバーでメッセージが記録される旨が
告知されています。また誰でも削除請求が可能です。

【保存期間について（上記の設問の補足）】
メッセージ行は30日で削除します。これは手動運用ではなく、定期実行されるジョブ
（src/miscellaneous/prune.js）によって強制されており、コードで検証できます。加えて保存範囲自体を
最小化しています。保存の対象は、削除・編集ログの出力先が設定されているサーバーのみです（当方の
テストサーバーには設定していないため、そこでは一切保存されません）。保存する項目はメッセージID、
投稿者ID、タイムスタンプ、および AES-256 で暗号化した本文と添付URLのみで、サーバーIDと
チャンネルIDは保存していません。また単一メッセージの削除が
ログに出力された時点で、該当行は30日を待たずに削除されます。Redis 上のキャッシュは
すべて3時間で失効します。

機械学習・AIモデルの学習、統計、プロファイリング、広告、分析への利用は一切ありません。メッセージ内容を
販売・貸与することもありません。主たる用途は、そのメッセージが投稿されたのと同じサーバーへ返すログ
埋め込みの生成です。

【当方サーバー外へ送信される経路（開示）】
1. エラー監視: 予期しないエラー発生時、エラーとスタックトレースを Sentry へ送信します。メッセージ本文を
   含める意図はありませんが、スタックトレースに断片が混入する可能性は否定できません。
2. メッセージのアーカイブ: /archive（メッセージ管理権限を持つモデレーターが実行）と一括削除ログは、
   対象メッセージを paste サービスへ書き出し、ログからリンクします。これはチャンネルを埋め尽くさずに
   大量のメッセージをモデレーターへ提示するための機能です。
これら以外に第三者へ送信されるものはありません。
```

#### 使用例を示すスクリーンショットや動画へのリンク（2000字）

```
<< 削除ログ（本文が写っているもの）／編集ログ Before-After／/ignorechannel／/stoplogging の4点のURL >>
```

### 3.4 Presence Intent セクション

**③でチェックしなければセクションごと出現しません。** 何も記入する必要はありません。

> ⚠️ **申請前に、Bot タブの PRESENCE INTENT トグルを OFF にしてください。**
> 現在 ON になっていますが、コード上まったく使用していません。申請で「使っていない」と書きながら
> 有効のままだと整合性を疑われます。OFF にしても本BOTの動作には影響しません。

### 3.5 判断が必要な項目：オプトアウトの設問

「ユーザーはメッセージ内容データの追跡をオプトアウトできますか？」は Yes/No のプルダウンのみで、
**その場に説明欄がありません**。

- **No（推奨）** — 事実そのままです。メンバー個人が自分だけログ対象外になる仕組みはありません。
  補足は「なぜMessage Content Intentが必要ですか？」欄に書きます（上記の文面に含めてあります）。
- **Yes** — 「管理者が /ignorechannel と /stoplogging で制御できる」ことを根拠にする解釈ですが、
  これは**サーバー単位の制御であってユーザー個人のオプトアウトではありません**。過大申告と
  受け取られた場合、送信時に同意する「情報が正確かつ完全である」という表明に反します。

**推奨は No です。** 監査ログBOTで個人がログを免れられないのは機能の性質上当然であり、
当方が運営するサーバーのみで動くプライベート運用という文脈では説明可能です。

---

## 4. 送信前の残作業

- [x] **プライバシーポリシー** — `PRIVACY.md`（英日併記）をリポジトリルートに作成。public リポジトリなので
      `https://github.com/waxsd100/RoboPolice/blob/master/PRIVACY.md` が恒久URLとして機能します。
      **PR #8 を master にマージした時点で有効になります。**
- [x] **利用規約** — `TERMS.md`。Developer Portal の Terms of Service URL 欄にも登録してください。
- [x] **BOT内からポリシーへ導線** — `/help`・`/info`・`/clearmydata` にポリシーURLを表示するよう変更。
      「Where is your Privacy Policy available?」への回答の裏付けになります。
- [x] **保持期間に関する記述を実態に合わせた** — 自動削除は実装しない方針のため、`PRIVACY.md` は
      「固定の保持期限は設けない」と明記。併せて、**上流 Logger 由来で `/clearmydata` と `/help` が
      「N日後に自動削除される」と表示していた虚偽の告知を削除**しました（実装が存在しないため）。
      `MESSAGE_HISTORY_DAYS` は `.env.example` 上で「BOTは強制しない」と注記。
- [ ] **スクリーンショット／動画** — 実フォームでは**インテントごとに別欄**があるので、2セット用意します。
      恒久URL（Imgur / YouTube限定公開 等）に上げてURLを貼ってください。

  **Server Members 欄に貼る4点**
  1. メンバー参加ログ（Account Age・Invite Used が写っているもの）
  2. 退出／キックログ（所持ロールと実行モデレーターが写っているもの）
  3. ニックネーム／ロール変更ログ
  4. `/userinfo` の実行結果

  **Message Content 欄に貼る4点**
  5. **メッセージ削除ログ（本文が写っているもの）= Message Content の用途証明。最重要**
  6. メッセージ編集ログの Before / After
  7. `/ignorechannel` の実行画面（管理者側の制御の証明）
  8. `/stoplogging` の実行画面

  **①アプリ説明欄にも画像リンクを貼れます**（「画像や動画の例へのリンクも自由にお使いいただけます」）。
  `/setup` の設定画面と `/help` のプライバシーポリシー欄を貼ると、説明の裏付けになります。
- [ ] **Developer Portal 側の登録** — General Information の Privacy Policy URL / Terms of Service URL に
      上記2つのURLを入力。
- [ ] **PRESENCE INTENT トグルを OFF にする** — 現在 ON だがコード上未使用。申請の記述と矛盾するため、
      送信前に必ず OFF にしてください（動作への影響はありません）。
- [ ] **オプトアウト設問の回答を決める** — §3.5。推奨は No。
- [ ] **Railway に `MESSAGE_HISTORY_DAYS=30` を設定** — これを入れないと削除ジョブが動かず、
      申請の「30日以内→はい」とポリシーの記述が事実と食い違います。§8 参照。
- [ ] **保持日数30日でよいか確認** — 変えるなら `PRIVACY.md`（4箇所）と §3 の回答も合わせて直します。
      30日以外にすると、フォームの「30日以内か」に Yes と答えられなくなる点だけ注意してください。

---

## 5. リポジトリと本番の乖離について（申請ブロッカーではない）

§1.1 の通り、本番は Message Content を受信できていますが、このリポジトリの pin（`eris@0.16.2-dev`,
commit `c075c5b`）では構造的に要求できません。**申請作業のためにコードを変更する必要はありません。**
むしろ、このリポジトリの状態に本番を合わせると本文ログが壊れます。

将来このリポジトリから再デプロイする場合にのみ、以下が必要になります。

1. `messageContent` 定数を持つ Eris（0.17 以降、またはそれ相当のフォーク）へ更新する
2. `src/bot/index.js` の intents に `messageContent` を加える。定数を持たない版を使い続けるなら
   数値で直接指定する：

   ```js
   // guilds(1) | guildMembers(2) | guildBans(4) | guildEmojis(8)
   // | guildInvites(64) | guildVoiceStates(128) | guildMessages(512)
   // | messageContent(1 << 15 = 32768)
   intents: 719 | (1 << 15), // = 33487
   ```

> ⚠️ 特権インテントが Portal で無効な状態でこのビットを送ると、ゲートウェイが
> `Disallowed intents specified` を返し（`Shard.js:2403`）**ボットが接続不能になります。**
> 万一審査で却下された場合は、この点に注意して設定を戻してください。

## 6. 申請時の注意点

- **却下は申請単位**です。インテントごとの部分承認はありません。不要な Presence を含めないこと。
- 「My bot needs message content」のような抽象的な説明は却下されます。**具体的な機能名と、
  それが特権インテント無しでは成立しない理由**を書くこと（上記テンプレートはその形式で書いてあります）。
- フォームには**下書き保存機能がありません**。本ファイルの回答を完成させてから、一気に貼り付けて送信してください。
- 却下されても再申請は可能です。ただし審査中もボットは通常通り稼働できます。
- 承認後も **毎年の再申請が必要**です。
- **プライベートBOTであることを隠さないでください。** 当方運営サーバーのみ・一般公開していない・
  ソース公開済み、という条件は審査上むしろ有利に働きます。ただし「1サーバーのみ」と
  盛らないこと（§7.1 A9）。

---

## 7. 敵対的検証（2026-09-13）

審査官が「この申請を落とす」つもりでコードと公開リポジトリを読んだ場合に突かれる箇所を洗い出し、
対処したものと、判断・確認が必要なものを分けて記録します。

### 7.1 修正済み

| # | 指摘 | 根拠 | 対処 |
|---|---|---|---|
| A1 | **提出するリポジトリ自体が「180,000サーバーで稼働」と書いてあった** | `package.json:4` | description を書き換え |
| A2 | README 冒頭が上流 Logger の説明で、**別のサポートサーバー**を案内していた | `README.md:1` | RoboPolice の説明に差し替え |
| A3 | `/help` `/info` が **`tizzysaurus/logger`** を「current code」として案内。提出するリポジトリと不一致（AGPL の観点でも不適切） | `slashcommands/help.js:21` ほか | `waxsd100/RoboPolice` に修正 |
| A4 | ポリシーの「データは自前インフラの外に出ない」が**虚偽**。Sentry へエラー送信、paste サイトへメッセージ本文送信 | `logger.js:5,14,22`, `index.js:12,126`, `archive.js:53`, `messageDeleteBulk.js:54` | PRIVACY.md と申請回答に開示を追加 |
| A5 | 「メッセージ削除時に行も削除される」が**一括削除では成立しない** | `messageDelete.js:20` にはあるが `messageDeleteBulk.js` に削除呼び出しが無い | 「単一削除のみ」と正確に記述 |
| A6 | `/ignorechannel` が遡及しないこと、**サーバー単位の削除が構造的に不可能**なこと（guild_id 未保存）を伏せていた | `generateDB.js` のスキーマ | PRIVACY.md に明記 |
| A7 | 「Database access is restricted to the App's own host」は**私が書いた未検証の主張** | — | 削除 |
| A8 | ToS の「メンバーには告知されている」も未検証の断定 | — | 「管理者が周知する責任を負う」に変更 |

| A9 | 「稼働しているのは1つのコミュニティサーバーのみ」という**事実と異なる断定**。実際には動作確認用の非公開テストサーバーにも設置している | 運用者からの申告 | 「当方が運営する少数のサーバーのみ」に修正。ポリシー・ToS・README・申請回答すべて |

| A10 | 「ログ管理しているのは1サーバーのみ」は**出力については真だが保存については偽だった**。`messageCreate.js` が出力先の設定を見ずに、全設置サーバーのメッセージを同じテーブルに保存していた | `messageCreate.js`（変更前） | 削除・編集ログの出力先が設定されたサーバーでのみ保存するよう変更（`GuildSettings.needsMessageCache()`）。保存も1サーバー分のみになった |

### 7.2 確認が必要（申請前に必ず）

- [ ] **Developer Portal の Public Bot トグルが OFF か。**
      ON なら「一般公開しておらず第三者が追加できない」という申請文が虚偽になります。**最重要。**
- [ ] **`SENTRY_URI` を本番で設定しているか。**
      未設定なら A4 の Sentry 段落は削除できます（開示が減る＝有利）。
- [ ] **`PASTE_SITE_ROOT_URL` を本番で設定しているか。設定しているなら自前ホストか。**
      未設定なら `/archive` と一括削除の本文書き出しは動作せず、A4 の該当段落を削除できます。
      第三者サービスを使っているなら、その事実をポリシーに明記する必要があります。
- [ ] **テストサーバーに外部のメンバーがいないか。**
      当方関係者のみなら問題ありません。第三者が参加しているサーバーがあるなら、そこでも実ユーザーの
      メッセージを収集していることになるため、ポリシーの適用範囲として意識してください
      （文面上はすでに全設置サーバーに適用されると明記済み）。
- [ ] **メンバーへのログ取得の告知が実際にあるか。** ルールチャンネル等にあるならスクリーンショットを
      申請に添付すると強い материал になります。

### 7.3 残る弱点（対処せず、承知のうえで出す）

1. ~~保存期間の上限が無い~~ → **解消。** 30日の自動削除を `src/miscellaneous/prune.js` で実装し、
   両セクションの「30日以内か」に Yes と答えられるようになりました。本申請で最も弱かった点です。
   なお `messages` にサーバーIDを保存していないため「サーバー単位でまとめて削除」は依然できませんが、
   30日で全件が失効するため実質的な問題は小さくなりました。
2. **`/invite` コマンドが招待リンクを2種類提示する。**
   プライベートBOTという説明と見た目が矛盾します。Public Bot が OFF なら他者は実際には追加できない
   ため実害はありませんが、スクリーンショットに写り込ませないでください。
3. **`/archive` は最大1000件のメッセージを外部 paste へ書き出す。**
   モデレーション用途として説明可能ですが、「用途はログ埋め込みの生成のみ」とは言えません。
   §3.3 の回答ではこの点を開示済みです。

---

## 8. データ保持の実装と Railway での運用

### 8.1 何を消しているか

| 対象 | 期限 | 実装 |
|---|---|---|
| PostgreSQL `messages` の行（削除・編集ログの出力先があるサーバーのみ保存） | `MESSAGE_HISTORY_DAYS` 日（既定30） | `src/miscellaneous/prune.js` |
| 単一削除されたメッセージの行 | 削除がログ出力された時点 | `src/bot/events/messageDelete.js:20` |
| PostgreSQL `guilds` の行 | BOTがサーバーから離脱した時点 | `src/bot/events/guildDelete.js:8` |
| Redis の招待／Webhook／設定キャッシュ | 3時間 | キー設定時の `EX` |

### 8.2 実行方式は2択（どちらか一方）

**方式A: BOT内で定期実行（既定・設定不要）**

shard 0 を持つワーカーが起動時に1回、以降1時間ごとに掃除します。`MESSAGE_HISTORY_DAYS` を
設定するだけで有効になります。Railway の変数に `MESSAGE_HISTORY_DAYS=30` を入れれば完了です。

**方式B: Railway の Cron サービス**

1. Railway のプロジェクトで **New Service → GitHub Repo** から同じリポジトリを追加
2. Settings → **Custom Start Command** に `node src/miscellaneous/prune.js`
3. Settings → **Cron Schedule** に `0 4 * * *`（UTC。毎日13時JST）
4. Variables に Postgres の変数を参照で設定（`${{Postgres.PGHOST}}` など）＋ `MESSAGE_HISTORY_DAYS=30`
5. **BOT側のサービスに `PRUNE_EXTERNAL=true` を設定**して、BOT内の定期実行を止める

`prune.js` は単体実行時に削除件数を出力して `exit 0` で終了するので、Railway Cron の
「実行して終了する」モデルに適合します。

> Railway の Cron は、前回の実行がまだ動いている場合は新しい実行を開始しません。初回の大量削除で
> 1時間以上かかるような場合でも、次回実行が重なることはありません。

**推奨は方式A**です。サービスが1つ増えず、変数の二重管理も不要で、BOTは常時起動しているためです。

### 8.3 保持期間を過ぎたメッセージの扱い

行が消えた後でも、**イベントが起きた事実は記録されます**（内容だけが出ません）。

| 操作 | 30日以内 | 30日経過後 |
|---|---|---|
| メッセージ削除 | 本文付きでログ出力 | 「削除された」ログを出力。Content 欄は「保持期間を過ぎているため取得不可」、日時とメッセージIDは表示 |
| メッセージ編集 | Before / After を出力 | 「編集された」ログを出力。Previous 欄は取得不可、投稿者・チャンネル・ジャンプリンクは表示 |
| 一括削除 | paste へ書き出してリンク | 「N件が一括削除された、内容は保持期間外」を出力 |

実装は `src/bot/utils/retention.js` の `isBeyondRetention()` です。メッセージIDの snowflake から
送信時刻を復元し、`MESSAGE_HISTORY_DAYS` を超えているかを判定します。

**「保持期間切れ」と「そもそも記録していない」を区別しています。** 行が無い原因は2つあり、
前者だけをログに出します。後者（`/logbots` が無効なときのBOTメッセージ、AutoMod のアクション、
BOT導入前や停止中のメッセージ）まで出すと、古い削除のたびにノイズが出てしまうためです。
`MESSAGE_HISTORY_DAYS` が未設定のときは削除自体が走らないので、この判定は常に false になります。

**モデレーション上の意味**: 31日以上前の発言を削除しても、内容は残りませんが**削除された事実は残ります**。
保持期間を延ばせば内容も残せますが、31日以上にするとフォームの「30日以内か」に Yes と答えられません。

### 8.4 初回実行時の注意

これまで一度も削除していないため、初回は**全期間分の古い行**が対象になります。`prune.js` は
5,000行ずつ、1回の実行あたり最大100万行までに制限しているので、バックログが大きい場合は
数回の実行に分かれて消化されます。Postgres のディスク使用量が段階的に下がることを確認してください。

大量削除後は、必要に応じて `VACUUM (ANALYZE) messages;` を実行すると領域が回収されます
（Railway のデータベースには autovacuum があるため、通常は不要です）。
